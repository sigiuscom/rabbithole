name: CD

on:
  push:
    branches: [main]
    paths-ignore: ['charts/**', 'VERSION', '**/*.md']
  workflow_dispatch:
    inputs:
      level:
        description: 'Version bump level override'
        required: false
        type: string
        default: 'auto'

permissions:
  contents: write
  packages: write

jobs:
  cd:
    uses: sigiuscom/workflows/.github/workflows/cd.yml@fe1d5d7c7eaf7bade8a9cc1bbc12d5a1f97af419  # main
    with:
      images: |
        [{"image":"ghcr.io/sigiuscom/rabbithole","context":".","dockerfile":"Dockerfile"}]
      level: ${{ inputs.level || 'auto' }}
    secrets: inherit
