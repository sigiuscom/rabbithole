name: CD

on:
  push:
    branches: [main]
    paths-ignore: ['charts/**', 'VERSION', '**/*.md']
  workflow_dispatch:
    inputs:
      level:
        description: 'Version bump level override'
        required: false
        type: string
        default: 'auto'

permissions:
  contents: write
  packages: write

jobs:
  cd:
    uses: sigiuscom/workflows/.github/workflows/cd.yml@8ea3068a2c3df0b8b85387803faed536b266673b  # main
    with:
      images: |
        [{"image":"ghcr.io/sigiuscom/rabbithole","context":".","dockerfile":"Dockerfile"}]
      level: ${{ inputs.level || 'auto' }}
    secrets: inherit
