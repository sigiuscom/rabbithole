name: CD

on:
  push:
    branches: [main]
    paths-ignore: ['charts/**', 'VERSION', '**/*.md']
  workflow_dispatch:
    inputs:
      level:
        description: 'Version bump level override'
        required: false
        type: string
        default: 'auto'

permissions:
  contents: write
  packages: write

jobs:
  cd:
    uses: sigiuscom/workflows/.github/workflows/cd.yml@13300b068bd61965e241b6d7ae4ba767fcc481d3  # main
    with:
      images: |
        [{"image":"ghcr.io/sigiuscom/rabbithole","context":".","dockerfile":"Dockerfile"}]
      level: ${{ inputs.level || 'auto' }}
    secrets: inherit
