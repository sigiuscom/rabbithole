name: CD

on:
  push:
    branches: [main]
    paths-ignore: ['charts/**', 'VERSION', '**/*.md']
  workflow_dispatch:
    inputs:
      level:
        description: 'Version bump level override'
        required: false
        type: string
        default: 'auto'

permissions:
  contents: write
  packages: write

jobs:
  cd:
    uses: sigiuscom/workflows/.github/workflows/cd.yml@59a1f44b553ea4f27d6c79108b4e8c9b432fc173  # main
    with:
      images: |
        [{"image":"ghcr.io/sigiuscom/rabbithole","context":".","dockerfile":"Dockerfile"}]
      level: ${{ inputs.level || 'auto' }}
    secrets: inherit
