#!/usr/bin/env sh
# --- BEGIN BEADS INTEGRATION (br) ---
# Export tracker data; preserve the caller's staged file selection.
if command -v br >/dev/null 2>&1 && [ -d .beads ]; then
  br sync --flush-only || exit "$?"
fi
# --- END BEADS INTEGRATION (br) ---
exit 0
